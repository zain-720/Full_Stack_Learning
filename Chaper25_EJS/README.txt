EJS is a templating language to let someone run javascript code directly in their HMTL (runs on .ejs not .html now)
(Folder 4.0 within) 

Skipping over learning EJS right now to save time

Whatever you need to know just directly search up for now, just search up how to run some kind of java script directly inside hmtl on GPT or something.